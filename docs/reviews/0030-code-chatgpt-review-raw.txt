[2026-07-19T10:58:56.156Z] count=6 generating=false
[2026-07-19T10:58:58.193Z] count=6 generating=false
=== CHATGPT REPLY (full) ===
V1.0.9 Registry Introspection — Code Review Report
Overall Verdict

总分：9.62 / 10

结论：✅ APPROVED（通过）

V1.0.9 Implementation 与 ADR-0030 设计高度一致，R1-R5 修订基本正确落地。该实现延续了 V1.0.6 → V1.0.8 的演进路线：

StageDescriptor（静态身份）
→ RuntimeMetadata（运行状态）
→ Metadata Access API（访问入口）
→ Stage Registry（生命周期管理）
→ Registry Introspection（可观测性）

架构方向正确，没有破坏 V1.x Core Freeze 原则。

没有 blocking issue。

建议合并。

1. 八维评分
维度	分数	评价
1. 架构方向	9.8/10	Introspection 放在 Registry 层正确，没有污染 Stage/Descriptor
2. API 一致性	9.6/10	API 风格延续 V1.0.8，命名清晰
3. 向后兼容	10/10	register() keyword extension，无破坏性变化
4. 数据结构设计	9.7/10	frozen dataclass + parallel metadata store 非常合理
5. 替代方案质量	9.5/10	Decision 1-7 大部分优秀
6. 测试策略	9.7/10	R4 schema stability + R5 AND test 很关键
7. R1-R5 落地	9.8/10	修订全部正确实施
8. V1.0.10 演化规划	9.2/10	方向正确，但 Serialization 需要更系统设计
总评
优点
1. 最大正确决定：_info 平行存储

这是整个实现最重要的架构选择。

当前：

StageRegistry

├── _stages
│   name -> Stage instance
│
├── _by_role
│
├── _by_capability
│
└── _info
    name -> StageInfo

非常正确。

原因：

StageDescriptor

描述：

Stage 自己是什么

StageInfo

描述：

Registry 如何管理它

两者不是同一个生命周期。

例如：

Python
Run
StageDescriptor(
    name="route",
    role="routing"
)

属于 Stage。

但是：

Python
Run
StageInfo(
    source="builtin",
    requires=("router",),
    registered_at=xxx
)

属于 Registry。

如果把 source/requires 塞进 StageDescriptor，会导致：

Descriptor 语义膨胀

Core Freeze 被破坏

Stage 自身 metadata 和 runtime context 混合

Decision 1：

accepted

评分：

10/10

Q1: StageInfo 设计是否正确？
结论：

✅ 正确，目前方案优于 alternatives。

三个方案比较：

方案	评价
Stage 持有 StageInfo	❌ Registry 污染 Stage
Registry 返回 tuple	❌ API 不稳定
Registry _info	✅ 最佳

当前：

Python
Run
_info: Dict[str, StageInfo]

是典型 Registry Metadata Pattern。

类似：

Kubernetes object metadata

Plugin registry metadata

Dependency container registration metadata

保持。

Q2: registered_at default_factory 是否正确？
结论：

✅ 正确。

代码：

Python
Run
registered_at: datetime = field(
    default_factory=datetime.now
)

这是 dataclass 推荐写法。

避免：

Python
Run
registered_at=datetime.now()

因为：

Python
Run
class A:
    time=datetime.now()

只执行一次。

唯一 minor：

timezone。

目前：

Python
Run
datetime.now()

产生：

naive datetime

未来如果 Registry 需要：

多机器同步

distributed registry

audit log

建议：

V1.1：

Python
Run
datetime.now(timezone.utc)

但是：

当前 V1.0：

不需要修改。

评分：

9.7/10

Q3: unknown source warning 是否过宽？
结论：

✅ 当前设计正确。

现在：

Python
Run
if source not in VALID_SOURCES:
    logger.warning(...)

比：

Python
Run
raise ValueError

更适合 V1.x。

原因：

Registry 是扩展系统。

未来可能：

Python
Run
source="company_internal_plugin"

或者：

Python
Run
source="community"

如果现在 reject：

会限制生态。

关于：

warnings.warn

我的建议：

不用。

原因：

logging 更适合：

server

plugin loading

CLI

DeprecationWarning 是 API 生命周期工具，不适合 metadata validation。

评分：

10/10

Q4: find_stages_needing AND 语义
结论：

✅ 正确。

代码：

Python
Run
dep_set.issubset(set(info.requires))

数学：

查询：

deps ⊆ requires

即：

Stage requires ALL deps

符合：

Python
Run
find_stages_needing(
    "router",
    "store"
)

含义：

找同时依赖 router 和 store 的 Stage

Edge case:

deps 重复

例如：

Python
Run
find_stages_needing(
 "router",
 "router"
)

set 后：

Python
Run
{"router"}

正确。

deps 空

当前：

Python
Run
if not deps:
    return []

正确。

另一种：

返回所有 Stage：

也合理。

但是：

API 名：

find_stages_needing()

空查询返回全部会危险。

保持。

评分：

10/10

Q5: to_dict schema stability
结论：

R4 是非常好的测试。

但是：

未来 V1.1 建议增加：

JSON
{
 "schema_version":1,
 "stages":[]
}

原因：

现在：

JSON
{
 stages,
 roles,
 capabilities
}

未来：

增加：

JSON
last_used_at
health
metrics

旧消费者不知道。

建议：

V1.0.10:

增加：

Python
Run
"schema_version": "1"

不要现在改。

评分：

9.3/10

Q6: _descriptor_to_dict 最佳位置？

三个方案：

A. StageDescriptor.to_dict()

目前：

不推荐。

原因：

StageDescriptor V1.0.6 已稳定。

改动：

影响范围：

core metadata

太大。

B. metadata_serialization.py

长期最佳。

例如：

planner/
├── metadata/
│
├── serialization.py

未来：

Python
Run
serialize_descriptor()
serialize_runtime_metadata()
serialize_stage_info()

统一。

C. 当前 helper

V1.0.9：

✅ 正确。

因为：

ADR-0030 是 Registry introspection。

不要提前抽象。

结论：

V1.0.9：

保持。

V1.0.10：

迁移到：

planner/metadata_serialization.py

评分：

9.5/10

Q7: VALID_SOURCES 是否需要 deprecated/experimental？
结论：

不同意加入。

保持：

Python
Run
{
"builtin",
"third_party",
"test"
}

原因：

source 表示：

来源

不是：

状态

所以：

错误：

experimental
deprecated

这些属于：

StageDescriptor：

例如：

Python
Run
experimental=True

或者：

RuntimeMetadata：

Python
Run
status="deprecated"

正确。

评分：

10/10

Q8: 测试覆盖盲点
当前覆盖：

很好。

40 tests 已覆盖：

✅ frozen
✅ backward compatibility
✅ cleanup
✅ serialization
✅ schema
✅ AND query
✅ default registry

补充建议：

(a) 多第三方 Stage

不是必须。

原因：

list_third_party 已验证。

(b) replace=True

已有：

test_replace_updates_info

够。

(c) indent=None

建议增加：

minor。

测试：

Python
Run
json.loads(
 registry.to_json(indent=None)
)

但是不是 blocking。

(d) OR mode

不要测试。

因为：

当前设计没有 OR。

未来：

find_stages_needing(
 deps,
 mode="any"
)

属于 V1.1。

评分：

9.7/10

ADR vs Code Discrepancies
Issue 1
registered_at None check

ADR：

Python
Run
info.registered_at.isoformat()

代码：

Python
Run
info.registered_at.isoformat()
if info.registered_at
else None
结论：

✅ Accept

理由：

这是增强防御。

没有改变 schema。

不是 ADR violation。

无需修改。

Issue 2
_NullStore 是否需要 ADR 声明？
结论：

建议补充，但非 blocking。

原因：

这是 ADR dependency chain。

建议：

ADR-0030 增加：

Dependency:
- V1.0.8 Rev1 _NullStore behavior

方便未来维护。

Issue 3
test 名不同

ADR:

test_register_unknown_source_allowed

代码：

test_register_unknown_source_warns_not_raises
结论：

不用修改。

代码名称更准确。

ADR 可以更新。

R1-R5 落地评价
Revision	状态
R1	✅
R2 source validation	✅ excellent
R3 default_factory	✅
R4 schema stability	✅ very good
R5 AND semantics	✅ important
必改项
无

Blocking:

None
Minor Suggestions
M1（建议）

增加 schema_version：

目标：

V1.1:

JSON
{
"schema_version":1,
"stages":[]
}
M2（建议）

datetime 改 UTC：

未来：

Python
Run
datetime.now(timezone.utc)
M3（建议）

ADR-0030 增加依赖声明：

Depends on V1.0.8 Rev1 _NullStore
V1.0.10 ADR-0031 路线建议

优先级：

P0
Metadata Serialization Layer

建立：

planner/
 └── metadata_serialization.py

负责：

Python
Run
StageDescriptor -> dict
StageInfo -> dict
RuntimeMetadata -> dict
P1

Schema versioning

加入：

JSON
schema_version
P2

UTC timestamp

P3

增强查询：

现在：

Python
Run
find_stages_needing()

未来：

Python
Run
find(
 role=None,
 capability=None,
 requires=None,
 source=None
)
Final Approval
V1.0.9 Registry Introspection

ADR: 9.62/10 Approved
Implementation: 9.62/10 Approved

Tests:
398/398 PASS

Decision:
✅ MERGE

Blocking:
NONE

Next:
Proceed V1.0.10 ADR-0031 Metadata Serialization

整体评价：

这是 V1.0.x 系列目前最成熟的一次扩展。

尤其 _info 平行 metadata store、R4 schema stability、R5 AND query 三个决定，会显著降低后续 Planner Runtime 演进成本。你目前的架构已经从“功能堆叠”进入“平台型 Registry 设计阶段”。
