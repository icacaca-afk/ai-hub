[2026-07-19T11:04:17.978Z] count=7 generating=true
[2026-07-19T11:04:20.872Z] count=7 generating=true
[2026-07-19T11:04:24.421Z] count=7 generating=true
[2026-07-19T11:04:27.279Z] count=7 generating=false
=== CHATGPT REPLY (full) ===
V1.0.10 ADR-0031 Metadata Serialization — ChatGPT ADR Review
总评

最终评分：9.6 / 10

结论：✅ APPROVED（优秀，通过）

ADR-0031 是 V1.0.6～V1.0.9 metadata 体系自然演进的一步。核心判断正确：

Metadata 已经从单一对象演化为 Registry / Runtime / Descriptor 多维状态，继续让每个模块自行序列化会产生 schema 漂移，因此需要一个独立 serialization boundary。

本 ADR 最大优点：

没有过早引入 schema version

没有扩大 Core Freeze 破坏面

保留 backward compatibility

serialization responsibility 从 Registry 中剥离

为 V1.0.11 Pipeline Introspection 和 CLI Introspection 提供基础设施

整体达到 V1.0.x 架构成熟阶段应有水平。

1. 八个维度评分
维度	评分	评价
1. 架构方向	9.8/10	独立 serialization layer 是正确方向，SRP 边界清晰
2. API 一致性	9.5/10	to_dict() + serialize_xxx() 双 API 略增加表面积，但合理
3. 向后兼容	9.8/10	delegate + deprecated alias 策略优秀
4. 数据结构/schema	9.6/10	R4 stability 思路正确，暂不引 schema_version 明智
5. Reject Alternatives	9.5/10	大部分拒绝理由充分
6. 测试策略	9.4/10	覆盖全面，建议增加 property testing
7. 实施计划	9.7/10	五阶段拆解合理，风险低
8. V1.1 演化规划	9.6/10	deferred 策略正确，没有提前设计

平均：

⭐ 9.6 / 10
2. Q1-Q8 逐项结论
Q1: 序列化模块位置

候选：

planner/metadata_serialization.py

planner/metadata/serialization.py

planner/serialize.py

结论：

✅ 当前：

planner/metadata_serialization.py

最佳。

评分：

9.8/10

原因：

当前阶段：

planner/
 ├── stage_descriptor.py
 ├── runtime_metadata.py
 ├── stage_registry.py
 └── metadata_serialization.py

符合 V1.0.x 扁平结构。

不建议现在升级：

planner/metadata/
    descriptor.py
    runtime.py
    serialization.py

原因：

这是 V2 级别模块化。

当前 metadata 还没有形成稳定 package boundary。

建议：

保持：

Python
Run
planner.metadata_serialization

V1.1/V2 如果出现：

MetadataSchema
MetadataMigration
MetadataVersion
MetadataValidator

再升级：

planner.metadata/
Q2: to_dict() + serialize_xxx() 双 API 是否过设计？
结论：

✅ 保留。

评分：

9.5/10

这是少数情况下双 API 是合理的。

原因：

两个用户群：

使用者：
Python
Run
descriptor.to_dict()

自然。

例如：

CLI:

Python
Run
print(stage.to_dict())

框架内部：

Python
Run
[
 serialize_descriptor(d)
 for d in descriptors
]

更明确。

但建议：

在 ADR 增加一个约束：

serialize_xxx() 是 canonical implementation，to_dict() 永远只是 facade。

避免未来：

Python
Run
StageDescriptor.to_dict()

里面出现业务逻辑。

Q3: StageDescriptor.to_dict() 是否破坏 Core Freeze？
结论：

✅ 不破坏。

评分：

9.9/10

理由：

V1.0.6 freeze 的核心应该理解为：

冻结：

字段

构造方式

语义

不是冻结：

方法扩展

例如：

Python
Run
@dataclass(frozen=True)
class StageDescriptor:

增加：

Python
Run
def to_dict()

不会改变：

Python
Run
hash()
eq()
immutability()

因此：

属于：

additive API extension

不是 breaking change。

Q4: lazy import 是否合理？
结论：

✅ 当前方案最佳。

评分：

9.7/10

当前：

Python
Run
def to_dict(self):
    from planner.metadata_serialization import serialize_descriptor

正确。

原因：

避免：

stage_descriptor
        |
        v
metadata_serialization
        |
        v
stage_descriptor

循环。

不建议 Protocol。

原因：

Protocol 解决的是：

interface dependency

不是：

module cycle。

这里 lazy import 更简单。

Q5: ConditionEval.to_dict() 保留还是移除？
结论：

选择：

✅ (a) 保留 method

评分：

9.8/10

原因：

ConditionEval 是最早 metadata API：

V1.0.4

已经存在。

删除：

Python
Run
ce.to_dict()

属于 unnecessary breaking change。

不建议：

Python
Run
DeprecationWarning

原因：

目前它不是错误 API。

只是实现迁移。

建议：

保持：

V1.0.10:
delegate

V1.1:
继续保留

V2:
再考虑删除
Q6: RuntimeMetadata 是否合并 ctx.metadata？
结论：

选择：

✅ (a)

评分：

9.9/10

这是一个非常重要的边界。

RuntimeMetadata:

回答：

execution runtime state

ctx.metadata:

回答：

arbitrary user context

混合：

会导致：

Python
Run
RuntimeMetadata.to_dict()

变成：

dump everything。

违反：

Single Responsibility。

未来如果需要：

增加：

Python
Run
ctx.to_dict()

或者：

Python
Run
serialize_execution_context(ctx)

不要污染 RuntimeMetadata。

Q7: serialize_xxx 是否加入 schema_version 参数？
结论：

❌ 不加入。

评分：

9.7/10

当前：

Python
Run
serialize_descriptor(d)

最佳。

不要：

Python
Run
serialize_descriptor(
    d,
    schema_version=1
)

原因：

V1.0.x 只有一个 schema。

提前：

Python
Run
if schema_version==1:

会制造未来负担。

V1.1:

可以演进：

方案：

Python
Run
serialize_descriptor(
    d,
    schema=MetadataSchema.V1
)

而不是裸 int。

Q8: 测试覆盖是否足够？
结论：

基本足够。

评分：

9.4/10

30 tests 合理。

已有覆盖：

✅ nested serialization

✅ frozenset deterministic

✅ datetime ISO

✅ None handling

✅ schema stability

✅ backward compatibility

建议增加两个测试：

1. round-trip stability

新增：

Python
Run
def test_json_round_trip():
    data = serialize_descriptor(d)

    text = to_json(data)

    assert json.loads(text)==data

价值：

验证 JSON boundary。

2. no mutation property

虽然已有：

Python
Run
immutable_input

建议扩展：

Python
Run
before = copy.deepcopy(rm)

serialize_runtime_metadata(rm)

assert rm == before
3. 必改项（Blocking）

只有：

Blocking-1
修正 ADR 中代码示例错误

当前 ADR 大量出现：

例如：

Python
Run
has_side_effeways_run_after_stop
Python
Run
serialize_desccriptor
Python
Run
return {
"stage":,
}

这些明显是复制污染。

正式 merge 前必须清理。

否则：

文档可信度下降。

Blocking-2

明确 canonical API

增加：

ADR §3.2:

建议补充：

serialize_xxx() is canonical serialization implementation. to_dict() methods are convenience wrappers only.

4. 建议修改（Non-blocking）
建议1

增加 typing:

目前：

Python
Run
Dict[str,Any]

可以接受。

但未来：

建议：

Python
Run
TypedDict

例如：

Python
Run
class StageDescriptorDict(TypedDict):
    name:str
    role:str

V1.1 可做。

建议2

METADATA_SCHEMA_VERSION

当前：

Python
Run
None

可以保留。

但名字略像已启用。

建议：

改：

Python
Run
CURRENT_METADATA_SCHEMA_VERSION = None

或者：

Python
Run
FUTURE_METADATA_SCHEMA_VERSION = None

减少误解。

建议3

StageRegistry.to_dict()

建议：

内部直接：

Python
Run
info.to_dict()

还是：

Python
Run
serialize_stage_info(info)

二选一。

当前：

Python
Run
serialize_stage_info()

更符合 canonical 原则。

保持即可。

5. V1.0.10 合并建议

推荐 commit 顺序：

commit 1:
add planner/metadata_serialization.py

commit 2:
add to_dict() facade methods

commit 3:
migrate ConditionEval

commit 4:
migrate StageRegistry helper

commit 5:
tests + regression

不要一次大 commit。

6. V1.0.11 ADR-0032 建议路线
推荐优先级：
P0
ADR-0032 Pipeline Introspection

理由：

当前链路：

StageDescriptor
        |
StageRegistry
        |
Metadata Serialization

已经具备：

describe 单 stage

下一步自然：

Python
Run
pipeline.describe()
pipeline.graph()

输出：

JSON
{
 "stages":[
 ],
 "edges":[
 ]
}
P1

ADR-0033 Predicate API

推荐：

Python
Run
registry.find(
    role="planner",
    capability="routing"
)

原因：

Registry 已经有：

roles

capabilities

requires

现在缺：

统一查询。

P2

ADR-0034 CLI Introspection

例如：

Bash
ai-hub stage list

ai-hub stage info router

ai-hub stage dump --json

依赖：

0031 serialization

0032 pipeline introspection

最终批准意见
ADR-0031 Metadata Serialization

状态：

✅ APPROVED

评分：

9.6 / 10

合并前：

必须：

修复 ADR 文档代码 typo / 截断错误

明确 serialize_xxx 是 canonical API

除此之外：

架构方向正确，可以进入 V1.0.10 implementation。

这一次 ADR 的意义其实比表面大：

V1.0.6～V1.0.9 是“建立 metadata”。

V1.0.10 开始是：

把 metadata 变成可被系统外部消费的稳定协议层。

这是从内部框架走向平台化的一个关键节点。
